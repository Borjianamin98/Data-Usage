<UserControl
    x:Class="DataUsage.Controls.DateUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DataUsage.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Viewbox x:Name="ViewBoxDay" Stretch="Uniform">
            <TextBlock x:Name="lblDay" FontSize="{x:Bind ViewModel.MainFont, Mode=OneWay}" Padding="0" Margin="0"/>
        </Viewbox>
        <TextBlock x:Name="lblMoreInfo" TextAlignment="Center" Grid.Row="1" FontSize="{x:Bind ViewModel.MainFont, Mode=OneWay}"/>
    </Grid>
</UserControl>
